const {User_kredensial} = require('../models')
const bcrypt = require('bcrypt')
const jwt = require('jsonwebtoken');


module.exports = {
    Login: async (req, res) => {
      
    },

    Me: async (req, res) => {
       
    },

    Logout: (req, res) => {
      
      
  }
}